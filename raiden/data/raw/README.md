Put human-curated conversations here (JSONL, same schema as data/schemas/sft.schema.json).
`scripts/prepare_dataset.py` currently synthesizes the Stage I candidate.
Merge raw files later by concatenating into processed/ before train.
